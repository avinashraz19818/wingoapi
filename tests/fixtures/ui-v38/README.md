# v38 original winning artwork recovery

See source.json: native asset404 on13L; matching original recovered from DhaniWin. No generated/redrawn image.
The artwork bytes are stored in .w13l/tools/13l-art38.json inside the delivery ZIP, validated by fixed SHA and135480-byte size before installation at the original path.
The extracted Winning component and its scoped CSS are unchanged native source. Tests use production Vue; native open/result fields,3s auto-close, toggle and manual-close behavior are executed. Only Lottie/Howler/API dependencies are stubbed, so no wagers/settlement or audio playback happens in this fixture.
Loss/close WebP files here are identical original assets on both sites, used only by the local fixture, not deployed changes.
results.json records six viewport widths; screenshots are local fixtures, not logged-in phone proof.
